import { GameType, Player1, Player2, Player } from "./types";

export const PLAYER1 = 1 as Player1;
export const PLAYER2 = 2 as Player2;
export const AUTOMATICMODE = "automatic" as GameType;
export const AUTOMATICPLAYERNAME = "machine" as Player;
